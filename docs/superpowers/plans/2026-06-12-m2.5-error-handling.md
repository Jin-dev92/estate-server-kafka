# M2.5 — 전역 에러 처리 + 커스텀 예외 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **문서 규칙:** 예시 코드는 구현 중에만 참고용으로 유지하고, PR 작성 직전에 제거했다(산문·시그니처·명령만 남김). 실제 구현은 각 파일과 커밋 이력 참조.

**Goal:** 흩어진 하드코딩 에러(`throw new NotFoundException('post not found')`)를 **컨텍스트별 에러 카탈로그 + 커스텀 예외 + 전역 ExceptionFilter**로 일원화해, FE가 4xx/5xx를 `{ statusCode, code, message, path, timestamp }` 일관 구조로 받게 한다.

**Architecture:** 중앙 메커니즘(`src/common/errors/`)에 `AppException`(HttpException 확장)·`DomainError`(순수 TS)·전역 필터를 두고, 컨텍스트별 에러 코드는 각 폴더에 코로케이션. 도메인은 NestJS를 모르므로 `DomainError`를 던지고 필터가 422로 매핑. 성공 응답은 불변, 에러만 봉투로 통일.

**Tech Stack:** NestJS 11, TypeScript, Jest. (스펙: `docs/superpowers/specs/2026-06-12-error-handling-design.md`)

**선행:** M2(Board)까지 포함된 상태(이 브랜치는 M2 위에 스택). 인프라(docker) 기동.

---

## 파일 구조

```
src/common/errors/error-response.ts                Create  ErrorResponse 타입(봉투)
src/common/errors/app-exception.ts                 Create  AppException + AppErrorSpec
src/common/errors/domain-error.ts                  Create  DomainError(순수 TS)
src/common/errors/all-exceptions.filter.ts         Create  전역 ExceptionFilter
src/common/errors/all-exceptions.filter.spec.ts    Test
src/common/errors/domain-error.spec.ts             Test
src/auth/auth.errors.ts                            Create  Auth 카탈로그
src/property/property.errors.ts                    Create  Property 카탈로그
src/board/board.errors.ts                          Create  Board 카탈로그
src/app.module.ts                                  Modify  APP_FILTER 전역 등록
src/auth/** (use-case·guard·repo·entity)           Modify  throw 이주 + spec 갱신
src/property/** (use-case·entity)                  Modify  throw 이주 + spec 갱신
src/board/** (use-case·entity)                     Modify  throw 이주 + spec 갱신
test/error-handling.e2e-spec.ts                    Create  에러 봉투 형태 + status 불변 검증
README.md                                          Modify  에러 응답 형식 문서
```

> 전역 등록은 `main.ts`의 `useGlobalFilters` 대신 **AppModule의 `APP_FILTER` provider**로 한다 — 이렇게 하면 `Test.createTestingModule`로 만드는 **e2e 테스트 앱에도 필터가 자동 적용**된다(main.ts useGlobalFilters는 실제 부팅에만 적용됨).

---

## Task 1: 중앙 에러 메커니즘 (AppException · DomainError · ErrorResponse)

**Files:** Create `error-response.ts`, `app-exception.ts`, `domain-error.ts` (모두 `src/common/errors/`), Test `domain-error.spec.ts`.

- [ ] **Step 1: `error-response.ts` (봉투 타입)** — `ErrorResponse` 인터페이스: `statusCode: number`, `code: string`, `message: string`, `path: string`, `timestamp: string`. 4xx/5xx 전용, FE 계약.

- [ ] **Step 2: `app-exception.ts` (비즈니스 예외)** — `AppErrorSpec { code: string; status: HttpStatus; message: string }` 인터페이스 + `AppException extends HttpException`. 생성자가 스펙을 받아 `super(spec.message, spec.status)` 호출하고 `readonly code`를 보유.

- [ ] **Step 3: 실패 테스트 — `domain-error.spec.ts`** — `DomainError`가 `Error` 인스턴스이며, code 미지정 시 기본값 `VALIDATION_FAILED`, code 명시 시 그 값을 갖는지 검증(2 케이스).

- [ ] **Step 4: 실패 확인** — `npx jest src/common/errors/domain-error.spec.ts` → FAIL(module 없음).

- [ ] **Step 5: `domain-error.ts` (순수 TS — NestJS import 금지)** — `DomainError extends Error`, 생성자 `(message: string, code = 'VALIDATION_FAILED')`, `name='DomainError'` + `readonly code`. 이 파일은 프레임워크 의존 0(도메인 엔티티가 직접 import).

- [ ] **Step 6: 통과 확인** — 동일 Run → PASS (2 passed). `npx tsc --noEmit` 에러 없음.

- [ ] **Step 7: Commit**

```bash
git add src/common/errors/error-response.ts src/common/errors/app-exception.ts src/common/errors/domain-error.ts src/common/errors/domain-error.spec.ts
git commit -m "[M2.5]feat: 중앙 에러 메커니즘 추가 (AppException, DomainError, ErrorResponse)"
```

---

## Task 2: 전역 ExceptionFilter + APP_FILTER 등록

**Files:** Create `all-exceptions.filter.ts`, Test `all-exceptions.filter.spec.ts`. Modify `src/app.module.ts`.

- [ ] **Step 1: 실패 테스트 — `all-exceptions.filter.spec.ts`** — `ArgumentsHost`를 mock(`switchToHttp → getResponse({status→json})`, `getRequest({url})`)하고 다음 매핑을 검증: ①`AppException` → 그 status·code·message 봉투, ②`DomainError` → 422 + `VALIDATION_FAILED`, ③Nest 기본 `NotFoundException(404)` → 파생 코드 `HTTP_404`, ④알 수 없는 `Error` → 500 `COMMON_INTERNAL_ERROR`.

- [ ] **Step 2: 실패 확인** — `npx jest src/common/errors/all-exceptions.filter.spec.ts` → FAIL.

- [ ] **Step 3: `all-exceptions.filter.ts`** — `@Catch()` 전역 필터. `catch()`에서 요청 url로 봉투를 만들고 `statusCode >= 500`이면 stack과 함께 로깅 후 `res.status().json()`. `toErrorResponse()`의 분기 순서(중요): `AppException`(HttpException 하위라 먼저) → `DomainError`(422) → `HttpException`(`getStatus()` + `deriveCode` + 메시지 추출) → 그 외 500. `deriveCode`는 400→`COMMON_VALIDATION_FAILED`, 401→`COMMON_UNAUTHORIZED`, 그 외 `HTTP_<status>`. `extractMessage`는 string/`{message}`/배열(ValidationPipe) 응답을 모두 처리.

- [ ] **Step 4: 통과 확인** — 동일 Run → PASS (4 passed).

- [ ] **Step 5: `src/app.module.ts`에 전역 필터 등록** — `providers` 배열에 `{ provide: APP_FILTER, useClass: AllExceptionsFilter }` 추가(`imports`는 그대로). `APP_FILTER`(`@nestjs/core`)로 등록하면 실제 부팅과 모든 e2e 테스트 앱에 자동 적용된다.

- [ ] **Step 6: 빌드 + 전체 테스트(기존 status 기반 e2e가 깨지지 않는지)**

```bash
npx tsc --noEmit && npx jest && npx jest --config ./test/jest-e2e.json
```
Expected: 전부 PASS. (AppException은 아직 안 쓰지만 필터가 Nest 기본 예외도 통과시키므로 기존 status 검증 e2e 불변.)

- [ ] **Step 7: Commit**

```bash
git add src/common/errors/all-exceptions.filter.ts src/common/errors/all-exceptions.filter.spec.ts src/app.module.ts
git commit -m "[M2.5]feat: 전역 AllExceptionsFilter 추가 및 APP_FILTER 등록"
```

---

## Task 3: 도메인 에러 이주 (DomainError)

도메인 엔티티의 `throw new Error('… is required')`(14곳)를 `DomainError`로 바꾸고 한국어 메시지로 교체. 엔티티 spec의 메시지/타입 단언을 `DomainError`로 전환.

**Files:** Modify `src/auth/domain/user.entity.ts`, `src/property/domain/{building,unit,lease}.entity.ts`, `src/board/domain/{post,comment}.entity.ts` + 각 `*.entity.spec.ts`.

- [ ] **Step 1: 엔티티 throw 교체 (import + 메시지)**

각 엔티티 상단에 `import { DomainError } from '../../common/errors/domain-error';`(경로는 깊이에 맞게: 도메인은 `src/<ctx>/domain/`이므로 `../../common/errors/domain-error`). 교체 목록:

- `user.entity.ts`: `Error('email is required')`→`DomainError('이메일은 필수입니다.')`, `Error('name is required')`→`DomainError('이름은 필수입니다.')`
- `building.entity.ts`: `ownerId`→`DomainError('건물 소유자 ID는 필수입니다.')`, `name`→`DomainError('건물 이름은 필수입니다.')`
- `unit.entity.ts`: `buildingId`→`DomainError('건물 ID는 필수입니다.')`, `name`→`DomainError('호실 이름은 필수입니다.')`
- `lease.entity.ts`: `unitId`→`DomainError('호실 ID는 필수입니다.')`, `tenantId`→`DomainError('입주자 ID는 필수입니다.')`
- `post.entity.ts`: `buildingId`→`DomainError('건물 ID는 필수입니다.')`, `authorId`→`DomainError('작성자 ID는 필수입니다.')`, `title`(create·edit 2곳)→`DomainError('제목은 필수입니다.')`, `content`(create·edit 2곳)→`DomainError('본문은 필수입니다.')`
- `comment.entity.ts`: `postId`→`DomainError('게시글 ID는 필수입니다.')`, `authorId`→`DomainError('작성자 ID는 필수입니다.')`, `content`→`DomainError('내용은 필수입니다.')`

- [ ] **Step 2: 엔티티 spec 단언 전환**

기존 메시지 문자열 단언(`toThrow('title is required')` 등)을 **`toThrow(DomainError)`** 로 바꾼다(메시지 언어에 독립적). 해당 spec 파일 상단에 `import { DomainError } from '../../common/errors/domain-error';` 추가.
- `src/board/domain/post.entity.spec.ts`: `toThrow('title is required')` → `toThrow(DomainError)`
- `src/auth/domain/user.entity.spec.ts`: `toThrow('email is required')` → `toThrow(DomainError)`
- `src/property/domain/building.entity.spec.ts`: `toThrow('ownerId is required')` → `toThrow(DomainError)`
(나머지 엔티티 spec에 동일 패턴이 있으면 같이 전환.)

- [ ] **Step 3: 도메인 테스트 통과 확인**

```bash
npx jest src/auth/domain src/property/domain src/board/domain
```
Expected: 전부 PASS.

- [ ] **Step 4: Commit**

```bash
git add src/auth/domain src/property/domain src/board/domain
git commit -m "[M2.5]refactor: 도메인 불변식 위반을 DomainError(한국어 메시지)로 이주"
```

---

## Task 4: Auth 카탈로그 + throw 이주 + 테스트

**Files:** Create `src/auth/auth.errors.ts`. Modify `sign-up.use-case.ts`, `login.use-case.ts`, `interface/roles.guard.ts`, `infrastructure/prisma-user.repository.ts` + 관련 spec.

- [ ] **Step 1: `auth.errors.ts`** — `AuthError` const(`satisfies Record<string, AppErrorSpec>`) 카탈로그: `EMAIL_IN_USE`(409, '이미 사용 중인 이메일입니다.'), `INVALID_CREDENTIALS`(401, '이메일 또는 비밀번호가 올바르지 않습니다.'), `INSUFFICIENT_ROLE`(403, '권한이 없습니다.').

- [ ] **Step 2: throw 이주**

각 파일에 `import { AppException } from '../../common/errors/app-exception';`(깊이에 맞게) + `import { AuthError } from '../auth.errors';`(또는 상대경로) 추가 후:
- `sign-up.use-case.ts`: `throw new ConflictException('email already in use')` → `throw new AppException(AuthError.EMAIL_IN_USE)` (`ConflictException` import 제거)
- `infrastructure/prisma-user.repository.ts`: 동일(`ConflictException('email already in use')` → `AppException(AuthError.EMAIL_IN_USE)`)
- `login.use-case.ts`: 2곳 `throw new UnauthorizedException('invalid credentials')` → `throw new AppException(AuthError.INVALID_CREDENTIALS)`
- `interface/roles.guard.ts`: `throw new ForbiddenException('insufficient role')` → `throw new AppException(AuthError.INSUFFICIENT_ROLE)`

> 보안: login의 "없는 이메일"·"비번 불일치"가 같은 `INVALID_CREDENTIALS`로 유지돼 이메일 존재 여부 미노출(기존 동작 보존).

- [ ] **Step 3: spec 단언 전환**

- `sign-up.use-case.spec.ts`: `rejects.toThrow('email already in use')` → `rejects.toMatchObject({ code: 'AUTH_EMAIL_IN_USE' })`
- `login.use-case.spec.ts`: `rejects.toThrow('invalid credentials')`(2곳) → `rejects.toMatchObject({ code: 'AUTH_INVALID_CREDENTIALS' })`
- `roles.guard.spec.ts`: `toThrow(ForbiddenException)` → `toThrow(AppException)` (그리고 필요 시 `code` 확인). `AppException` import 추가, `ForbiddenException` import 제거.

- [ ] **Step 4: 테스트 통과 확인**

```bash
npx jest src/auth
```
Expected: 전부 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/auth
git commit -m "[M2.5]refactor: Auth 에러 카탈로그 + AppException 이주 및 테스트 전환"
```

---

## Task 5: Property 카탈로그 + throw 이주 + 테스트

**Files:** Create `src/property/property.errors.ts`. Modify `create-unit.use-case.ts`, `issue-invite-code.use-case.ts`, `redeem-invite-code.use-case.ts` + 관련 spec.

- [ ] **Step 1: `property.errors.ts`** — `PropertyError` const(`satisfies Record<string, AppErrorSpec>`) 카탈로그: `BUILDING_NOT_FOUND`(404, '건물을 찾을 수 없습니다.'), `NOT_BUILDING_OWNER`(403, '건물 소유자만 할 수 있습니다.'), `UNIT_NOT_FOUND`(404, '호실을 찾을 수 없습니다.'), `INVALID_INVITE_CODE`(404, '유효하지 않거나 만료된 초대코드입니다.').

- [ ] **Step 2: throw 이주** (각 파일에 `AppException`·`PropertyError` import 추가, Nest 예외 import 제거)
- `create-unit.use-case.ts`: `NotFoundException('building not found')` → `AppException(PropertyError.BUILDING_NOT_FOUND)`; `ForbiddenException('not the building owner')` → `AppException(PropertyError.NOT_BUILDING_OWNER)`
- `issue-invite-code.use-case.ts`: `NotFoundException('unit not found')` → `AppException(PropertyError.UNIT_NOT_FOUND)`; `ForbiddenException('not the building owner')` → `AppException(PropertyError.NOT_BUILDING_OWNER)`
- `redeem-invite-code.use-case.ts`: `NotFoundException('invalid or expired invite code')` → `AppException(PropertyError.INVALID_INVITE_CODE)`

- [ ] **Step 3: spec 단언 전환** (클래스 단언 → code 단언)
- `create-unit.use-case.spec.ts`: `rejects.toThrow(ForbiddenException)` → `rejects.toMatchObject({ code: 'PROPERTY_NOT_BUILDING_OWNER' })`; `rejects.toThrow(NotFoundException)` → `rejects.toMatchObject({ code: 'PROPERTY_BUILDING_NOT_FOUND' })`
- `issue-invite-code.use-case.spec.ts`: `ForbiddenException` → `{ code: 'PROPERTY_NOT_BUILDING_OWNER' }`; `NotFoundException` → `{ code: 'PROPERTY_UNIT_NOT_FOUND' }`
- `redeem-invite-code.use-case.spec.ts`: `NotFoundException` → `{ code: 'PROPERTY_INVALID_INVITE_CODE' }`

- [ ] **Step 4: 테스트 통과 확인** — `npx jest src/property` → PASS.

- [ ] **Step 5: Commit**

```bash
git add src/property
git commit -m "[M2.5]refactor: Property 에러 카탈로그 + AppException 이주 및 테스트 전환"
```

---

## Task 6: Board 카탈로그 + throw 이주 + 테스트

**Files:** Create `src/board/board.errors.ts`. Modify `create-post`, `list-posts`, `get-post`, `update-post`, `delete-post`, `create-comment` use-cases + 관련 spec.

- [ ] **Step 1: `board.errors.ts`** — `BoardError` const(`satisfies Record<string, AppErrorSpec>`) 카탈로그: `POST_NOT_FOUND`(404, '게시글을 찾을 수 없습니다.'), `NOT_AUTHOR`(403, '글 작성자만 수정·삭제할 수 있습니다.'), `NOT_BUILDING_MEMBER`(403, '해당 건물의 멤버가 아닙니다.').

- [ ] **Step 2: throw 이주** (각 파일 `AppException`·`BoardError` import, Nest 예외 import 제거)
- `create-post.use-case.ts`: `ForbiddenException('not a building member')` → `AppException(BoardError.NOT_BUILDING_MEMBER)`
- `list-posts.use-case.ts`: 동일 → `NOT_BUILDING_MEMBER`
- `get-post.use-case.ts`: `NotFoundException('post not found')` → `POST_NOT_FOUND`; `ForbiddenException('not a building member')` → `NOT_BUILDING_MEMBER`
- `update-post.use-case.ts`: `NotFoundException('post not found')` → `POST_NOT_FOUND`; `ForbiddenException('not the author')` → `NOT_AUTHOR`
- `delete-post.use-case.ts`: 동일 두 개(`POST_NOT_FOUND`, `NOT_AUTHOR`)
- `create-comment.use-case.ts`: `NotFoundException('post not found')` → `POST_NOT_FOUND`; `ForbiddenException('not a building member')` → `NOT_BUILDING_MEMBER`

- [ ] **Step 3: spec 단언 전환**
- `list-posts.use-case.spec.ts`: `rejects.toThrow(ForbiddenException)` → `rejects.toMatchObject({ code: 'BOARD_NOT_BUILDING_MEMBER' })`
- `get-post.use-case.spec.ts`: `NotFoundException` → `{ code: 'BOARD_POST_NOT_FOUND' }`; `ForbiddenException`(2곳: miss·cache-hit) → `{ code: 'BOARD_NOT_BUILDING_MEMBER' }`
- `update-post.use-case.spec.ts`: `ForbiddenException` → `{ code: 'BOARD_NOT_AUTHOR' }`; `NotFoundException` → `{ code: 'BOARD_POST_NOT_FOUND' }`
- `create-comment.use-case.spec.ts`: `NotFoundException` → `{ code: 'BOARD_POST_NOT_FOUND' }`; `ForbiddenException` → `{ code: 'BOARD_NOT_BUILDING_MEMBER' }`

- [ ] **Step 4: 빌드 + 전체 단위 테스트** — `npx tsc --noEmit && npx jest` → 컴파일 에러 없음, 전부 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/board
git commit -m "[M2.5]refactor: Board 에러 카탈로그 + AppException 이주 및 테스트 전환"
```

---

## Task 7: e2e — 에러 봉투 형태 + status 불변

**Files:** Create `test/error-handling.e2e-spec.ts`.

> 봉투(`code`)를 검증하려면 필터가 활성화돼야 한다 — `APP_FILTER`로 등록했으므로 `Test.createTestingModule({ imports: [AppModule] })`만으로 자동 적용된다. 도메인 422는 DTO ValidationPipe가 빈 입력을 400으로 먼저 막아 HTTP로 트리거하기 어려우므로, e2e는 400/401 봉투를 검증한다.

- [ ] **Step 1: 실패 e2e 작성** — `AppModule`을 import해 앱을 띄우고 `ValidationPipe`를 등록한 뒤 다음을 검증: ①잘못된 로그인 → 401 + `code: AUTH_INVALID_CREDENTIALS`이고 `statusCode/message/path/timestamp` 필드가 봉투 형태, ②DTO 검증 실패(짧은 비밀번호) → 400 + `COMMON_VALIDATION_FAILED`, ③토큰 없이 보호 엔드포인트(`/auth/me`) → 401 + `COMMON_UNAUTHORIZED` 봉투. `afterAll`에서 테스트 유저 정리.

- [ ] **Step 2: 인프라 확인 후 e2e 실행**

```bash
docker compose up -d
npx jest --config ./test/jest-e2e.json
```
Expected: 신규 error-handling e2e + 기존 auth·property·board·redis-script e2e 전부 PASS(기존은 status 기반이라 불변).

- [ ] **Step 3: Commit**

```bash
git add test/error-handling.e2e-spec.ts
git commit -m "[M2.5]test: 에러 봉투 형태 및 status 불변 e2e 추가"
```

---

## Task 8: 마무리 검증 & README 에러 형식 문서

**Files:** Modify `README.md`.

- [ ] **Step 1: 전체 검증**

```bash
npm run lint && npx jest && npx jest --config ./test/jest-e2e.json
```
Expected: lint 0 errors, 모든 단위·e2e PASS.

- [ ] **Step 2: README "API 레퍼런스" 섹션에 "에러 응답 형식" 하위 추가**

API 표 아래에, 4xx/5xx 공통 에러 봉투 형태(`{ statusCode, code, message, path, timestamp }`)와 대표 `code` 목록(예: `AUTH_INVALID_CREDENTIALS`, `BOARD_POST_NOT_FOUND`, `BOARD_NOT_BUILDING_MEMBER`, `COMMON_VALIDATION_FAILED` 등)을 한 단락+표로 기재. "FE는 `code`로 분기"를 명시.

- [ ] **Step 3: 마일스톤 표 갱신** — README 마일스톤 표에 `M2.5 — 전역 에러 처리` 행 추가(M2와 M3 사이) + ✅.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "[M2.5]docs: README에 에러 응답 형식·코드 표 추가 및 마일스톤 갱신"
```

---

## M2.5 완료 기준 (Definition of Done)

- [ ] 4xx/5xx 응답이 `{ statusCode, code, message, path, timestamp }` 봉투로 통일됨(전역 필터, APP_FILTER 등록)
- [ ] 비즈니스 에러는 `AppException`(컨텍스트 카탈로그), 도메인 불변식 위반은 `DomainError`(422)로 통일
- [ ] 24개 application throw + 14개 도메인 throw 전부 이주, 하드코딩 메시지 제거(메시지는 카탈로그/한국어)
- [ ] 단위 테스트가 메시지 문자열/Nest 예외 클래스 대신 **`code`/`DomainError`** 로 검증
- [ ] 코드↔HTTP status 1:1 매핑으로 **기존 status 기반 e2e 불변**, 신규 봉투 e2e 통과
- [ ] `DomainError`/도메인 엔티티가 NestJS를 import하지 않음(도메인 순수성 유지)
- [ ] lint 0 errors, 전체 단위·e2e PASS

---

## Self-Review 결과

- **스펙 커버리지:** 스펙 3절(봉투)→Task 1·2, 4.1(중앙)→Task 1·2, 4.2(카탈로그)→Task 4·5·6, 4.3(매핑)→Task 4·5·6, 5절(필터 규칙)→Task 2, 6절(이주·테스트)→Task 3~7. 모두 커버.
- **placeholder 스캔:** TBD/모호 표현 없음. 이주 대상은 파일·라인별 before→after로 명시.
- **타입 일관성:** `AppErrorSpec{code,status,message}`가 `AppException`·세 카탈로그에서 동일. `DomainError(message, code='VALIDATION_FAILED')` 시그니처가 도메인·필터·테스트에서 일치. `ErrorResponse` 필드가 필터·e2e에서 일치. 코드 문자열(`AUTH_*`·`PROPERTY_*`·`BOARD_*`·`COMMON_*`·`VALIDATION_FAILED`)이 카탈로그·필터·테스트 단언에서 일치.
- **범위 밖(스펙 8절):** 성공 응답 래핑·숫자 코드 체계·i18n·rate limit·모니터링 연동 제외.

# FE-M4 1:1 채팅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 건물주·입주자가 socket.io로 실시간 1:1 채팅을 하고, 방 목록에서 마지막 메시지를 최근순으로 본다.

**Architecture:** 읽기(방 목록·히스토리)는 Server Component가 `getToken()`으로 BE를 직접 호출. 메시지 전송은 클라이언트 컴포넌트의 socket.io. 방 생성(ensure)은 Next Route Handler 프록시. socket.io 핸드셰이크 JWT는 Server Component가 읽어 클라이언트에 `token` prop으로 전달.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript · socket.io-client · Vitest(FE) / Jest(BE) · NestJS(BE).

**스펙:** `docs/superpowers/specs/frontend/2026-06-23-fe-m4-chat-design.md`

## Global Constraints

- `.ts`/`.tsx`만 생성. Server Component 기본, `"use client"`는 상호작용 컴포넌트에만 최소 범위.
- 매직 스트링 금지: 페이지 경로 `PAGE_ROUTES`, 내부 API 경로 `API_ROUTES`(`lib/constants.ts`), 사용자 문구 `MESSAGES`(`lib/messages.ts`).
- `enum` 금지(as const), `as any` 금지, index signature 금지.
- BE 호출은 `lib/api/<domain>.ts` + 배럴 `lib/api/index.ts`. 소비자는 `@/lib/api`에서 import.
- `useEffect` deps는 원시값만.
- FE 테스트: `npm run test`(Vitest). BE 테스트: `npm test`(Jest, estate-server에서).
- 민감 키를 `NEXT_PUBLIC_`에 넣지 않는다. `NEXT_PUBLIC_WS_URL`은 BE 공개 호스트(비밀 아님)라 허용.
- 작업 레포 경로: FE = `estate-web`(현재 cwd), BE = `../estate-server`.
- 커밋 메시지 형식: `type: 내용`(`feature`/`fix`/`refactor`/`test`/`chore`/`docs`).

**Before you start:** FE는 `feature/fe-m4-chat` 브랜치, BE(Task 1)는 estate-server에서 별도 `feature/chat-room-list-last-message` 브랜치에서 작업한다(둘 다 base `dev`, PR only).

---

### Task 1: (BE) 방 목록 응답에 lastMessage + 최근순 정렬

**레포: `../estate-server`** — 방 목록 API가 `{id, buildingId, ownerId, tenantId}`만 주어 마지막 메시지·최근순이 없다. `ListRoomsUseCase`가 방별 최근 1건(캐시 우선·DB 폴백)을 조립하고 최근순 정렬한다.

**Files:**
- Modify: `../estate-server/src/chat/application/list-rooms.use-case.ts`
- Modify: `../estate-server/src/chat/interface/chat.controller.ts:63-74` (`myRooms`)
- Test: `../estate-server/src/chat/application/list-rooms.use-case.spec.ts` (신규)

**Interfaces:**
- Produces (REST): `GET /chat/rooms` → `{ id, buildingId, ownerId, tenantId, lastMessage: { content: string; createdAt: string } | null }[]`, `lastMessage.createdAt` 내림차순(없는 방은 뒤).
- Produces (use-case): `ListRoomsUseCase.execute(userId): Promise<RoomSummary[]>` where `RoomSummary = { room: ChatRoom; lastMessage: ChatMessagePayload | null }`.

- [ ] **Step 1: 실패 테스트 작성** — `../estate-server/src/chat/application/list-rooms.use-case.spec.ts`

```ts
import { ListRoomsUseCase } from './list-rooms.use-case';
import { ChatRoom } from '../domain/chat-room.entity';
import { ChatRoomRepository } from '../domain/chat-room.repository';
import { MessageCache } from '../domain/message-cache';
import { MessageRepository } from '../domain/message.repository';
import { ChatMessagePayload } from '../domain/chat-message';

const OWNER = 'owner1';
function room(id: string, tenantId: string) {
  return ChatRoom.reconstitute({ id, buildingId: 'b1', ownerId: OWNER, tenantId });
}
function payload(roomId: string, createdAt: string): ChatMessagePayload {
  return { roomId, messageId: `m-${roomId}`, senderId: OWNER, content: `c-${roomId}`, createdAt };
}

function build(opts: {
  rooms: ChatRoom[];
  cacheByRoom?: Record<string, ChatMessagePayload[]>;
}) {
  const rooms: Partial<ChatRoomRepository> = {
    findByParticipant: () => Promise.resolve(opts.rooms),
  };
  const cache: Partial<MessageCache> = {
    getRecent: (roomId: string) => Promise.resolve(opts.cacheByRoom?.[roomId] ?? []),
  };
  const messages: Partial<MessageRepository> = {
    findRecent: () => Promise.resolve([]),
  };
  return new ListRoomsUseCase(
    rooms as ChatRoomRepository,
    cache as MessageCache,
    messages as MessageRepository,
  );
}

describe('ListRoomsUseCase', () => {
  it('각 방의 마지막 메시지를 붙인다', async () => {
    const useCase = build({
      rooms: [room('r1', 't1')],
      cacheByRoom: { r1: [payload('r1', '2026-06-20T00:00:00.000Z')] },
    });
    const result = await useCase.execute(OWNER);
    expect(result[0].room.id).toBe('r1');
    expect(result[0].lastMessage?.content).toBe('c-r1');
  });

  it('마지막 메시지 시각 내림차순으로 정렬한다(없는 방은 뒤)', async () => {
    const useCase = build({
      rooms: [room('old', 't1'), room('none', 't2'), room('new', 't3')],
      cacheByRoom: {
        old: [payload('old', '2026-06-10T00:00:00.000Z')],
        new: [payload('new', '2026-06-22T00:00:00.000Z')],
      },
    });
    const result = await useCase.execute(OWNER);
    expect(result.map((r) => r.room.id)).toEqual(['new', 'old', 'none']);
    expect(result[2].lastMessage).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ../estate-server && npm test -- list-rooms`
Expected: FAIL (`ListRoomsUseCase` 생성자 인자 수 불일치 / lastMessage 없음)

- [ ] **Step 3: 유스케이스 구현** — `../estate-server/src/chat/application/list-rooms.use-case.ts` 전체 교체

```ts
import { Inject, Injectable } from '@nestjs/common';
import { ChatRoom } from '../domain/chat-room.entity';
import {
  CHAT_ROOM_REPOSITORY,
  ChatRoomRepository,
} from '../domain/chat-room.repository';
import { MESSAGE_CACHE, MessageCache } from '../domain/message-cache';
import {
  MESSAGE_REPOSITORY,
  MessageRepository,
} from '../domain/message.repository';
import { ChatMessagePayload } from '../domain/chat-message';

export interface RoomSummary {
  room: ChatRoom;
  lastMessage: ChatMessagePayload | null;
}

@Injectable()
export class ListRoomsUseCase {
  constructor(
    @Inject(CHAT_ROOM_REPOSITORY) private readonly rooms: ChatRoomRepository,
    @Inject(MESSAGE_CACHE) private readonly cache: MessageCache,
    @Inject(MESSAGE_REPOSITORY) private readonly messages: MessageRepository,
  ) {}

  // 본인이 참가자(owner 또는 tenant)인 방 목록 + 마지막 메시지(최근순).
  async execute(userId: string): Promise<RoomSummary[]> {
    const rooms = await this.rooms.findByParticipant(userId);
    const summaries = await Promise.all(
      rooms.map(async (room) => ({
        room,
        lastMessage: await this.lastMessage(room.id),
      })),
    );
    // 마지막 메시지 시각 내림차순(없는 방은 뒤로).
    return summaries.sort((a, b) => {
      const at = a.lastMessage?.createdAt ?? '';
      const bt = b.lastMessage?.createdAt ?? '';
      return bt.localeCompare(at);
    });
  }

  // 캐시 우선, 비었으면 DB 폴백(get-messages와 동일 전략).
  private async lastMessage(roomId: string): Promise<ChatMessagePayload | null> {
    const cached = await this.cache.getRecent(roomId, 1);
    if (cached.length > 0) return cached[0];
    const rows = await this.messages.findRecent(roomId, 1);
    if (rows.length === 0) return null;
    const m = rows[0];
    return {
      roomId: m.roomId,
      messageId: m.id,
      senderId: m.senderId,
      content: m.content,
      createdAt: m.createdAt.toISOString(),
    };
  }
}
```

- [ ] **Step 4: 컨트롤러 매핑 확장** — `../estate-server/src/chat/interface/chat.controller.ts`의 `myRooms` 교체

```ts
  @Get('rooms')
  @ApiOperation({ summary: '내 채팅방 목록(마지막 메시지·최근순)' })
  @ApiResponse({ status: 200, description: '방 목록' })
  async myRooms(@CurrentUser() user: TokenPayload) {
    const summaries = await this.listRooms.execute(user.sub);
    return summaries.map(({ room, lastMessage }) => ({
      id: room.id,
      buildingId: room.buildingId,
      ownerId: room.ownerId,
      tenantId: room.tenantId,
      lastMessage: lastMessage
        ? { content: lastMessage.content, createdAt: lastMessage.createdAt }
        : null,
    }));
  }
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd ../estate-server && npm test -- list-rooms`
Expected: PASS (2 tests)

- [ ] **Step 6: 커밋**

```bash
cd ../estate-server
git add src/chat/application/list-rooms.use-case.ts src/chat/application/list-rooms.use-case.spec.ts src/chat/interface/chat.controller.ts
git commit -m "feature: 채팅 방 목록에 마지막 메시지·최근순 정렬 추가"
```

---

### Task 2: (FE) chat API 모듈 + 의존성 + 상수/메시지

socket.io-client 설치, `lib/api/chat.ts`를 실제 계약으로 확장(기존 stub 교체), 상수·메시지 추가, 기존 소비자(`lib/dashboard.ts`) 갱신.

**Files:**
- Modify: `package.json` (deps: `socket.io-client`)
- Modify: `lib/api/chat.ts` (전체 교체)
- Modify: `lib/dashboard.ts:2,24` (`backendChatRooms`→`backendMyRooms`)
- Modify: `lib/constants.ts` (`API_ROUTES.chatRooms`, `PAGE_ROUTES.chatRoom`)
- Modify: `lib/messages.ts` (`MESSAGES.chat`)
- Test: `lib/chat-api.test.ts` (신규)

**Interfaces:**
- Produces: `ChatRoom = { id; buildingId; ownerId; tenantId; lastMessage: { content: string; createdAt: string } | null }`
- Produces: `ChatMessage = { roomId; messageId; senderId; content; createdAt }` (모두 string)
- Produces: `backendMyRooms(t): Promise<ChatRoom[]>`, `backendRoomMessages(t, roomId, limit?=50): Promise<ChatMessage[]>`, `backendEnsureRoom(t, { buildingId, tenantId }): Promise<ChatRoom>`
- Produces: `API_ROUTES.chatRooms = "/api/chat/rooms"`, `PAGE_ROUTES.chatRoom(id) = "/chat/:id"`
- Produces: `MESSAGES.chat.{empty,connectFailed,notParticipant,startFailed,inputPlaceholder,startOwner,disconnected}`

- [ ] **Step 1: socket.io-client 설치**

Run: `npm install socket.io-client`
Expected: `package.json` dependencies에 `socket.io-client` 추가, 설치 성공.

- [ ] **Step 2: 실패 테스트 작성** — `lib/chat-api.test.ts`

```ts
import { vi } from "vitest";
import { backendMyRooms, backendRoomMessages, backendEnsureRoom } from "@/lib/api";

it("backendMyRooms: /chat/rooms를 Bearer로 호출한다", async () => {
  const fetchMock = vi.fn(async () => new Response(JSON.stringify([]), { status: 200 }));
  vi.stubGlobal("fetch", fetchMock);
  await backendMyRooms("tok");
  const [url, init] = fetchMock.mock.calls[0];
  expect(String(url)).toMatch(/\/chat\/rooms$/);
  expect((init as RequestInit).method).toBe("GET");
});

it("backendRoomMessages: roomId·limit 쿼리를 붙인다", async () => {
  const fetchMock = vi.fn(async () => new Response(JSON.stringify([]), { status: 200 }));
  vi.stubGlobal("fetch", fetchMock);
  await backendRoomMessages("tok", "r1", 50);
  expect(String(fetchMock.mock.calls[0][0])).toMatch(/\/chat\/rooms\/r1\/messages\?limit=50$/);
});

it("backendEnsureRoom: POST body로 buildingId·tenantId를 보낸다", async () => {
  const fetchMock = vi.fn(async () => new Response(JSON.stringify({ id: "r1" }), { status: 201 }));
  vi.stubGlobal("fetch", fetchMock);
  await backendEnsureRoom("tok", { buildingId: "b1", tenantId: "t1" });
  const init = fetchMock.mock.calls[0][1] as RequestInit;
  expect(init.method).toBe("POST");
  expect(JSON.parse(String(init.body))).toEqual({ buildingId: "b1", tenantId: "t1" });
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm run test -- chat-api`
Expected: FAIL (`backendMyRooms`/`backendRoomMessages`/`backendEnsureRoom` export 없음)

- [ ] **Step 4: `lib/api/chat.ts` 전체 교체**

```ts
import { call, authGet } from "./client";

export type ChatRoom = {
  id: string;
  buildingId: string;
  ownerId: string;
  tenantId: string;
  lastMessage: { content: string; createdAt: string } | null;
};

export type ChatMessage = {
  roomId: string;
  messageId: string;
  senderId: string;
  content: string;
  createdAt: string; // ISO 8601
};

export const backendMyRooms = (t: string) => authGet<ChatRoom[]>("/chat/rooms", t);

export const backendRoomMessages = (t: string, roomId: string, limit = 50) =>
  authGet<ChatMessage[]>(`/chat/rooms/${roomId}/messages?limit=${limit}`, t);

export const backendEnsureRoom = (t: string, body: { buildingId: string; tenantId: string }) =>
  call<ChatRoom>("/chat/rooms", {
    method: "POST",
    headers: { Authorization: `Bearer ${t}` },
    body: JSON.stringify(body),
  }, {});
```

- [ ] **Step 5: 기존 소비자 갱신** — `lib/dashboard.ts`

`lib/dashboard.ts:2`의 import에서 `backendChatRooms` → `backendMyRooms`로 바꾸고, `lib/dashboard.ts:24`의 호출도 `safe(backendMyRooms(token), [] as ChatRoom[])`로 변경.

- [ ] **Step 6: 상수 추가** — `lib/constants.ts`

`API_ROUTES` 객체에 추가:
```ts
  chatRooms: "/api/chat/rooms",
```
`PAGE_ROUTES` 객체에서 기존 `chat: "/chat"` 아래에 추가:
```ts
  chatRoom: (id: string) => `/chat/${id}`,
```

- [ ] **Step 7: 메시지 추가** — `lib/messages.ts`의 `MESSAGES` 객체에 `comment` 다음 항목으로 추가

```ts
  chat: {
    empty: "진행 중인 대화가 없어요.",
    connectFailed: "실시간 연결에 실패했어요. 잠시 후 다시 시도해주세요.",
    disconnected: "연결이 끊어졌어요. 재연결 중…",
    notParticipant: "이 대화의 참가자가 아니에요.",
    startFailed: "대화를 시작하지 못했어요. 잠시 후 다시 시도해주세요.",
    inputPlaceholder: "메시지를 입력하세요",
    startOwner: "건물주에게 문의하기",
  },
```

- [ ] **Step 8: 테스트·빌드 확인**

Run: `npm run test -- chat-api && npm run lint`
Expected: chat-api 3 tests PASS, lint 통과.

- [ ] **Step 9: 커밋**

```bash
git add package.json package-lock.json lib/api/chat.ts lib/dashboard.ts lib/constants.ts lib/messages.ts lib/chat-api.test.ts
git commit -m "feature: 채팅 API 모듈·상수·메시지·socket.io-client 추가"
```

---

### Task 3: (FE) 순수 헬퍼 — 방 라벨 · 히스토리 정렬

socket 부수효과는 테스트 제외하고, 테스트 가능한 순수 로직만 분리한다.

**Files:**
- Create: `lib/chat/room-label.ts`
- Create: `lib/chat/ws.ts`
- Test: `lib/chat/room-label.test.ts`

**Interfaces:**
- Consumes: `ChatRoom`, `ChatMessage` (`@/lib/api`), `ROLE` (`@/lib/constants`)
- Produces: `chatRoomLabel(room: ChatRoom, myRole: "OWNER" | "TENANT" | "ADMIN", buildingNameById: Map<string,string>): string`
- Produces: `toAscending(messages: ChatMessage[]): ChatMessage[]` (BE는 최신순 → 오래된→최신 역순)
- Produces: `WS_URL: string` (`lib/chat/ws.ts`)

- [ ] **Step 1: 실패 테스트 작성** — `lib/chat/room-label.test.ts`

```ts
import { chatRoomLabel, toAscending } from "@/lib/chat/room-label";
import type { ChatRoom, ChatMessage } from "@/lib/api";

const room: ChatRoom = { id: "r1", buildingId: "b1", ownerId: "o1", tenantId: "t1", lastMessage: null };

it("OWNER에게는 상대가 입주자로 표기된다", () => {
  const names = new Map([["b1", "행복빌라"]]);
  expect(chatRoomLabel(room, "OWNER", names)).toBe("행복빌라 · 입주자");
});

it("TENANT에게는 상대가 건물주로 표기된다", () => {
  const names = new Map([["b1", "행복빌라"]]);
  expect(chatRoomLabel(room, "TENANT", names)).toBe("행복빌라 · 건물주");
});

it("건물명을 모르면 '채팅방'으로 폴백한다", () => {
  expect(chatRoomLabel(room, "OWNER", new Map())).toBe("채팅방 · 입주자");
});

it("toAscending: 최신순 히스토리를 오래된→최신으로 뒤집는다", () => {
  const msgs: ChatMessage[] = [
    { roomId: "r1", messageId: "m2", senderId: "o1", content: "two", createdAt: "2026-06-20T00:00:02.000Z" },
    { roomId: "r1", messageId: "m1", senderId: "o1", content: "one", createdAt: "2026-06-20T00:00:01.000Z" },
  ];
  expect(toAscending(msgs).map((m) => m.messageId)).toEqual(["m1", "m2"]);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test -- room-label`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현** — `lib/chat/room-label.ts`

```ts
import type { ChatRoom, ChatMessage } from "@/lib/api";
import { ROLE } from "@/lib/constants";

// 상대 역할: 내가 OWNER면 상대는 입주자, 그 외(TENANT)면 건물주.
function counterpartLabel(myRole: "OWNER" | "TENANT" | "ADMIN"): string {
  return myRole === ROLE.OWNER ? "입주자" : "건물주";
}

export function chatRoomLabel(
  room: ChatRoom,
  myRole: "OWNER" | "TENANT" | "ADMIN",
  buildingNameById: Map<string, string>,
): string {
  const building = buildingNameById.get(room.buildingId) ?? "채팅방";
  return `${building} · ${counterpartLabel(myRole)}`;
}

// BE 히스토리는 최신순 → 화면 표시용 오래된→최신.
export function toAscending(messages: ChatMessage[]): ChatMessage[] {
  return [...messages].reverse();
}
```

- [ ] **Step 4: WS URL 헬퍼** — `lib/chat/ws.ts`

```ts
// 클라이언트가 socket.io로 직접 연결하는 BE 공개 호스트(비밀 아님).
export const WS_URL = process.env.NEXT_PUBLIC_WS_URL ?? "http://localhost:3001";
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm run test -- room-label`
Expected: PASS (4 tests)

- [ ] **Step 6: 커밋**

```bash
git add lib/chat/room-label.ts lib/chat/room-label.test.ts lib/chat/ws.ts
git commit -m "feature: 채팅 방 라벨·히스토리 정렬 헬퍼 추가"
```

---

### Task 4: (FE) 방 목록 페이지 `/chat`

역할 인지 방 목록. buildingId→건물명 매핑 후 라벨·마지막 메시지 미리보기 렌더. TENANT가 방이 없으면 "건물주에게 문의" 버튼(Task 6의 `StartChatButton`)을 노출 — 단, Task 6 완료 전에는 빈 상태만 렌더하고 Task 6에서 버튼을 끼운다.

**Files:**
- Create: `app/chat/page.tsx`

**Interfaces:**
- Consumes: `backendMe`, `backendMyRooms`, `backendMyBuildings`, `backendMyLeases`, `ChatRoom` (`@/lib/api`); `chatRoomLabel` (`@/lib/chat/room-label`); `getToken` (`@/lib/session`); `PAGE_ROUTES`, `ROLE` (`@/lib/constants`); `AppShell`, `Card`, `EmptyState`, `ListRow`; `MESSAGES`.
- Produces: 라우트 `/chat`.

- [ ] **Step 1: 페이지 구현** — `app/chat/page.tsx`

```tsx
import { redirect } from "next/navigation";
import Link from "next/link";
import { getToken } from "@/lib/session";
import { backendMe, backendMyRooms, backendMyBuildings, backendMyLeases, type ChatRoom } from "@/lib/api";
import { chatRoomLabel } from "@/lib/chat/room-label";
import { AppShell } from "@/components/ui/app-shell";
import { Card } from "@/components/ui/card";
import { EmptyState } from "@/components/ui/empty-state";
import { ListRow } from "@/components/ui/list-row";
import { PAGE_ROUTES, ROLE } from "@/lib/constants";
import { MESSAGES } from "@/lib/messages";

export default async function ChatListPage() {
  const token = await getToken();
  if (!token) redirect(PAGE_ROUTES.login);

  let me;
  try {
    me = await backendMe(token);
  } catch {
    redirect(PAGE_ROUTES.login);
  }

  let rooms: ChatRoom[];
  try {
    rooms = await backendMyRooms(token);
  } catch {
    rooms = [];
  }

  // buildingId → 건물명 매핑(역할별 소스).
  const buildingNameById = new Map<string, string>();
  if (me.role === ROLE.OWNER) {
    try {
      (await backendMyBuildings(token)).forEach((b) => buildingNameById.set(b.id, b.name));
    } catch {}
  } else {
    try {
      (await backendMyLeases(token)).forEach((l) => {
        if (l.buildingId && l.buildingName) buildingNameById.set(l.buildingId, l.buildingName);
      });
    } catch {}
  }

  const initial = me.email.charAt(0).toUpperCase();

  return (
    <AppShell unread={0} userInitial={initial}>
      <h1 className="mb-4 text-[22px] font-extrabold tracking-tight">채팅</h1>
      {rooms.length === 0 ? (
        <EmptyState text={MESSAGES.chat.empty} />
      ) : (
        <Card className="p-0">
          <div className="divide-y divide-border px-4">
            {rooms.map((r) => (
              <Link key={r.id} href={PAGE_ROUTES.chatRoom(r.id)} className="block hover:bg-surface-2">
                <ListRow
                  title={chatRoomLabel(r, me.role, buildingNameById)}
                  desc={r.lastMessage?.content}
                  meta={r.lastMessage ? new Date(r.lastMessage.createdAt).toLocaleDateString("ko-KR") : undefined}
                />
              </Link>
            ))}
          </div>
        </Card>
      )}
    </AppShell>
  );
}
```

- [ ] **Step 2: 빌드·lint 확인**

Run: `npm run build && npm run lint`
Expected: 빌드 성공, `/chat` 라우트 포함, lint 통과.

- [ ] **Step 3: 커밋**

```bash
git add app/chat/page.tsx
git commit -m "feature: 채팅 방 목록 페이지(/chat) 추가"
```

---

### Task 5: (FE) 대화 페이지 `/chat/[roomId]` + 실시간 컴포넌트

Server Component가 히스토리·토큰을 페치해 클라이언트 `ChatConversation`에 전달. 클라이언트는 socket.io로 연결·join·수신·전송한다.

**Files:**
- Create: `app/chat/[roomId]/page.tsx`
- Create: `components/chat/chat-conversation.tsx`

**Interfaces:**
- Consumes: `backendMe`, `backendRoomMessages`, `ChatMessage` (`@/lib/api`); `toAscending` (`@/lib/chat/room-label`); `WS_URL` (`@/lib/chat/ws`); `getToken`; `PAGE_ROUTES`; `AppShell`; `MESSAGES`; `io` (`socket.io-client`).
- Produces: 라우트 `/chat/:roomId`. 컴포넌트 `ChatConversation({ token, roomId, myId, initial: ChatMessage[] })`.

- [ ] **Step 1: 실시간 클라이언트 컴포넌트** — `components/chat/chat-conversation.tsx`

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import { io, type Socket } from "socket.io-client";
import type { ChatMessage } from "@/lib/api";
import { WS_URL } from "@/lib/chat/ws";
import { MESSAGES } from "@/lib/messages";

type Props = { token: string; roomId: string; myId: string; initial: ChatMessage[] };

export function ChatConversation({ token, roomId, myId, initial }: Props) {
  const [messages, setMessages] = useState<ChatMessage[]>(initial);
  const [connected, setConnected] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [draft, setDraft] = useState("");
  const socketRef = useRef<Socket | null>(null);
  const bottomRef = useRef<HTMLDivElement | null>(null);

  useEffect(() => {
    const socket = io(WS_URL, { auth: { token }, transports: ["websocket"] });
    socketRef.current = socket;

    socket.on("connect", () => {
      setConnected(true);
      setError(null);
      socket.emit("join", { roomId });
    });
    socket.on("disconnect", () => setConnected(false));
    socket.on("connect_error", () => setError(MESSAGES.chat.connectFailed));
    socket.on("message", (m: ChatMessage) => {
      if (m.roomId !== roomId) return;
      // 히스토리·echo 중복 방지(messageId 기준).
      setMessages((prev) => (prev.some((x) => x.messageId === m.messageId) ? prev : [...prev, m]));
    });
    socket.on("error", (e: { code?: string; message?: string }) => {
      setError(e.code === "CHAT_NOT_ROOM_PARTICIPANT" ? MESSAGES.chat.notParticipant : (e.message ?? MESSAGES.chat.connectFailed));
    });

    return () => {
      socket.disconnect();
      socketRef.current = null;
    };
  }, [token, roomId]);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: "smooth" });
  }, [messages.length]);

  function send() {
    const content = draft.trim();
    if (!content || !socketRef.current) return;
    socketRef.current.emit("message", { roomId, content });
    setDraft("");
  }

  return (
    <div className="flex flex-col gap-3">
      {error && <p className="rounded-[12px] bg-danger/10 px-4 py-2 text-[13px] text-danger">{error}</p>}
      {!error && !connected && <p className="text-[13px] text-text-3">{MESSAGES.chat.disconnected}</p>}

      <div className="flex flex-col gap-2 pb-2">
        {messages.map((m) => {
          const mine = m.senderId === myId;
          return (
            <div key={m.messageId} className={`flex ${mine ? "justify-end" : "justify-start"}`}>
              <div className={`max-w-[78%] whitespace-pre-wrap rounded-[16px] px-4 py-2.5 text-[15px] ${mine ? "bg-brand-500 text-white" : "bg-surface-2 text-text"}`}>
                {m.content}
              </div>
            </div>
          );
        })}
        <div ref={bottomRef} />
      </div>

      <div className="sticky bottom-0 flex items-end gap-2 bg-bg pt-2">
        <textarea
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === "Enter" && !e.shiftKey) {
              e.preventDefault();
              send();
            }
          }}
          rows={1}
          placeholder={MESSAGES.chat.inputPlaceholder}
          className="flex-1 resize-none rounded-[14px] border border-border bg-surface px-4 py-3 text-[15px] text-text outline-none focus:border-brand-500 focus:ring-2 focus:ring-brand-50"
        />
        <button
          onClick={send}
          disabled={!connected || draft.trim().length === 0}
          className="h-[50px] shrink-0 rounded-[14px] bg-brand-500 px-5 font-bold text-[15px] text-white transition active:scale-[.985] disabled:opacity-50"
        >
          전송
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 대화 페이지** — `app/chat/[roomId]/page.tsx`

```tsx
import { redirect } from "next/navigation";
import { getToken } from "@/lib/session";
import { backendMe, backendRoomMessages, type ChatMessage } from "@/lib/api";
import { toAscending } from "@/lib/chat/room-label";
import { AppShell } from "@/components/ui/app-shell";
import { ChatConversation } from "@/components/chat/chat-conversation";
import { PAGE_ROUTES } from "@/lib/constants";

export default async function ChatRoomPage({
  params,
}: {
  params: Promise<{ roomId: string }>;
}) {
  const token = await getToken();
  if (!token) redirect(PAGE_ROUTES.login);

  const { roomId } = await params;

  let me;
  try {
    me = await backendMe(token);
  } catch {
    redirect(PAGE_ROUTES.login);
  }

  let history: ChatMessage[];
  try {
    history = await backendRoomMessages(token, roomId);
  } catch {
    history = [];
  }

  return (
    <AppShell unread={0} userInitial={me.email.charAt(0).toUpperCase()}>
      <ChatConversation token={token} roomId={roomId} myId={me.id} initial={toAscending(history)} />
    </AppShell>
  );
}
```

- [ ] **Step 3: 빌드·lint 확인**

Run: `npm run build && npm run lint`
Expected: 빌드 성공(`/chat/[roomId]` 동적 라우트 포함), lint 통과.

- [ ] **Step 4: 수동 실시간 검증** (BE 실행 중이어야 함; socket은 단위 테스트 제외)

1. 두 브라우저(건물주·입주자 세션)로 같은 방 `/chat/<roomId>` 접속.
2. 한쪽에서 메시지 전송 → 양쪽 화면에 즉시 표시(echo)되는지 확인.
3. 새로고침 시 히스토리가 오래된→최신 순으로 보이는지 확인.
4. 잘못된/만료 토큰이면 상단에 연결 실패 배너가 뜨는지 확인.

- [ ] **Step 5: 커밋**

```bash
git add app/chat/[roomId]/page.tsx components/chat/chat-conversation.tsx
git commit -m "feature: 실시간 대화 페이지(/chat/[roomId]) 추가"
```

---

### Task 6: (FE) 대화 시작(ensure) — Route Handler + 진입점 연결

ensure 프록시 라우트와 `StartChatButton`을 만들고, TENANT(방 목록 빈 상태)·OWNER(게시판 글 상세)에 끼운다.

**Files:**
- Create: `app/api/chat/rooms/route.ts`
- Create: `components/chat/start-chat-button.tsx`
- Test: `components/chat/start-chat-button.test.tsx`
- Modify: `app/chat/page.tsx` (TENANT 빈 상태에 버튼)
- Modify: `app/board/[buildingId]/[postId]/page.tsx` (OWNER에 "작성자와 채팅")

**Interfaces:**
- Consumes: `backendEnsureRoom` (`@/lib/api`); `getToken`; `API_ROUTES`, `PAGE_ROUTES`; `MESSAGES`; `Button`.
- Produces: Route `POST /api/chat/rooms` body `{buildingId, tenantId}` → `{id,...}`. 컴포넌트 `StartChatButton({ buildingId, tenantId, label })`.

- [ ] **Step 1: Route Handler** — `app/api/chat/rooms/route.ts`

```ts
import { NextRequest, NextResponse } from "next/server";
import { getToken } from "@/lib/session";
import { backendEnsureRoom, ApiError } from "@/lib/api";

export async function POST(req: NextRequest) {
  const token = await getToken();
  if (!token) return NextResponse.json({ message: "인증 필요" }, { status: 401 });
  try {
    const body = await req.json();
    const room = await backendEnsureRoom(token, body);
    return NextResponse.json(room, { status: 201 });
  } catch (e) {
    const err = e as ApiError;
    return NextResponse.json({ message: err.message, status: err.status }, { status: err.status ?? 500 });
  }
}
```

- [ ] **Step 2: 실패 테스트 작성** — `components/chat/start-chat-button.test.tsx`

```tsx
import { vi } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { StartChatButton } from "@/components/chat/start-chat-button";

const push = vi.fn();
vi.mock("next/navigation", () => ({ useRouter: () => ({ push }) }));

beforeEach(() => {
  push.mockReset();
});

it("성공 시 생성된 방으로 이동한다", async () => {
  vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ id: "r9" }), { status: 201 })));
  render(<StartChatButton buildingId="b1" tenantId="t1" label="문의하기" />);
  fireEvent.click(screen.getByText("문의하기"));
  await waitFor(() => expect(push).toHaveBeenCalledWith("/chat/r9"));
});

it("실패 시 에러 메시지를 표시한다", async () => {
  vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ message: "권한 없음" }), { status: 403 })));
  render(<StartChatButton buildingId="b1" tenantId="t1" label="문의하기" />);
  fireEvent.click(screen.getByText("문의하기"));
  await waitFor(() => expect(screen.getByText("권한 없음")).toBeInTheDocument());
  expect(push).not.toHaveBeenCalled();
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm run test -- start-chat-button`
Expected: FAIL (컴포넌트 없음)

- [ ] **Step 4: 컴포넌트 구현** — `components/chat/start-chat-button.tsx`

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { API_ROUTES, PAGE_ROUTES } from "@/lib/constants";
import { MESSAGES } from "@/lib/messages";

export function StartChatButton({
  buildingId,
  tenantId,
  label,
}: {
  buildingId: string;
  tenantId: string;
  label: string;
}) {
  const router = useRouter();
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function start() {
    setLoading(true);
    setError(null);
    const res = await fetch(API_ROUTES.chatRooms, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ buildingId, tenantId }),
    });
    if (res.ok) {
      const room = await res.json();
      router.push(PAGE_ROUTES.chatRoom(room.id));
    } else {
      const json = await res.json().catch(() => ({}));
      setError(json.message ?? MESSAGES.chat.startFailed);
      setLoading(false);
    }
  }

  return (
    <div className="mt-4">
      <Button onClick={start} disabled={loading}>
        {loading ? "여는 중…" : label}
      </Button>
      {error && <p className="mt-2 text-[13px] text-danger">{error}</p>}
    </div>
  );
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm run test -- start-chat-button`
Expected: PASS (2 tests)

- [ ] **Step 6: TENANT 진입점** — `app/chat/page.tsx` 수정

`backendMyLeases`로 가져온 리스를 재사용해, TENANT이고 방이 없을 때 활성 리스 건물로 시작 버튼을 노출한다. import에 `StartChatButton`·`LEASE_STATUS` 추가 후, TENANT 분기에서 활성 리스를 구해 빈 상태 아래에 버튼을 렌더:

```tsx
// import 추가
import { StartChatButton } from "@/components/chat/start-chat-button";
import { PAGE_ROUTES, ROLE, LEASE_STATUS } from "@/lib/constants";
```

TENANT 매핑 블록을 활성 리스도 함께 구하도록 교체:
```tsx
  let activeBuildingId: string | null = null;
  if (me.role === ROLE.OWNER) {
    try {
      (await backendMyBuildings(token)).forEach((b) => buildingNameById.set(b.id, b.name));
    } catch {}
  } else {
    try {
      const leases = await backendMyLeases(token);
      leases.forEach((l) => {
        if (l.buildingId && l.buildingName) buildingNameById.set(l.buildingId, l.buildingName);
      });
      activeBuildingId = leases.find((l) => l.status === LEASE_STATUS.ACTIVE && l.buildingId)?.buildingId ?? null;
    } catch {}
  }
```

빈 상태 렌더를 교체(입주자는 시작 버튼 노출):
```tsx
      {rooms.length === 0 ? (
        <>
          <EmptyState text={MESSAGES.chat.empty} />
          {me.role === ROLE.TENANT && activeBuildingId && (
            <StartChatButton buildingId={activeBuildingId} tenantId={me.id} label={MESSAGES.chat.startOwner} />
          )}
        </>
      ) : (
```

- [ ] **Step 7: OWNER 진입점** — `app/board/[buildingId]/[postId]/page.tsx` 수정

글 작성자가 본인이 아니면 "작성자와 채팅" 버튼을 노출(`tenantId = post.authorId`). import 추가 및 `me` 페치, 버튼 삽입:

```tsx
// import 추가
import { backendGetPost, backendMe } from "@/lib/api";
import { StartChatButton } from "@/components/chat/start-chat-button";
import { PAGE_ROUTES, POST_CATEGORY, POST_CATEGORY_LABEL, ROLE } from "@/lib/constants";
```

`post` 페치 직후 `me`도 페치(실패해도 글은 보이도록 degrade):
```tsx
  let me = null;
  try {
    me = await backendMe(token);
  } catch {}
```

본문 `Card` 닫힌 직후, 댓글 `section` 위에 삽입:
```tsx
      {me?.role === ROLE.OWNER && me.id !== post.authorId && (
        <StartChatButton buildingId={buildingId} tenantId={post.authorId} label="작성자와 채팅" />
      )}
```
(이때 `params`에서 `buildingId`도 구조분해: `const { buildingId, postId } = await params;`)

- [ ] **Step 8: 전체 테스트·빌드·lint**

Run: `npm run test && npm run build && npm run lint`
Expected: 전체 테스트 PASS, 빌드 성공, lint 통과.

- [ ] **Step 9: 커밋**

```bash
git add app/api/chat/rooms/route.ts components/chat/start-chat-button.tsx components/chat/start-chat-button.test.tsx app/chat/page.tsx "app/board/[buildingId]/[postId]/page.tsx"
git commit -m "feature: 대화 시작(ensure) 라우트·버튼 및 진입점 연결"
```

---

## 마무리 (계획 외 후속)

- README 마일스톤 표에서 FE-M3 ✅, FE-M4 ✅로 갱신(별도 docs 커밋).
- BE(Task 1)·FE는 각각 PR로 올린다. PR 본문에 이 계획(`docs/superpowers/plans/frontend/2026-06-23-fe-m4-chat.md`)과 스펙 경로를 첨부.
- 배포 환경에 `NEXT_PUBLIC_WS_URL`(BE 공개 호스트) 설정 필요.

## Self-Review 결과

- **스펙 커버리지:** §3 BE 보강→Task 1 / §4·§5 페이지·컴포넌트→Task 2~6 / §6 진입점→Task 6 / §7 에러→각 페이지·컴포넌트 / §8 테스트→Task 1·2·3·6. 모두 매핑됨.
- **플레이스홀더:** 없음(모든 step에 실제 코드/명령).
- **타입 일관성:** `ChatRoom`/`ChatMessage`(Task 2) → Task 3·4·5에서 동일 사용. `chatRoomLabel`/`toAscending` 시그니처 Task 3 정의 = Task 4·5 사용 일치. `backendEnsureRoom`/`backendMyRooms`/`backendRoomMessages` Task 2 정의 = 이후 사용 일치. `StartChatButton` props(Task 6) 일관.
